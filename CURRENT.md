# CURRENT.md

The live operational doc. If it is happening and it is not here, it is not happening. Weekly ritual per SOBO_PLAYBOOK.md section 8.

Last updated: 2026-07-11 (the parallel 2026-07-11 lines merged to one history: the V2 program built through Phase 5, the harvest-born 4I scheduling upgrade, the Ambition Angels demo engagement seed, and the agreement-review line whose gated 5B archive superseded the parallel paper export; see the top of Recently shipped. Standing FLAG from the parallel sessions: 0026_v2_ownership and 0026_v2_scheduling_upgrade share the 0026 number; they touch disjoint objects, lexical order keeps the apply deterministic, both are on the live project).

## Where things stand after the V2 close (2026-07-11)

Every plannable epic is shipped. What remains is not code, it is operations:
- **Remi's manual steps** (docs/setup-checklist.md): attach app.soboconsulting.com (section 4b: Vercel Domains plus the CNAME, then the Supabase URL allow-list and Google redirect URIs); the three keys (RESEND_API_KEY, ANTHROPIC_API_KEY, CRON_SECRET) that wake email, all five AI jobs, and the two crons; deactivate the four remi+ test personas before the real SafeSpace four arrive.
- **The 390px live runs** owed across the shipped surfaces.
- **1C (one real template)** stays deliberately parked until the first engagement completes; the closeout room now exists to produce that ending.
- **Deferred by decision, not debt:** per-recipient digest modes and the 3E composer picker (tenant-two era), 4G nav for practices that flip the pipeline flag on. The deliverable-promised queue source is UNBLOCKED as of 2026-07-13 (planned deliverables, migration 0035, gave promises structure); the 4A queue line itself stays unbuilt until Remi asks.

## State

- **Ring 0 (Preflight): done.** Findings in `docs/keystone-preflight.md`.
- **Ring 1 (The spine): built and applied to live infrastructure.** The pre-provisioned Supabase project (`keystone`) carries migrations 0001, 0002, and the SafeSpace seed (1 practice, 3 practice members, 1 client, 4 client members, 1 engagement, 5 workstreams). The pre-provisioned Vercel project (`keystone`, git-linked to this repo) builds previews from this branch with the public env values in vercel.json.
- **Ring 2 (Sessions and scheduling): built.** Migration 0003 applied locally and in CI; sessions, availability windows, the encrypted Google connection store, the pure slot engine (DST-pinned unit tests), client booking/reschedule/cancel (pure RLS plus the DB exclusion constraint), practice settings (windows, connect, sync).
- **Ring 3 (Notes, AI extraction, homework): built.** Migration 0005 applied to the live project. Transcript paste, opus extraction into an inert ai_proposals row, the single accept path publishing notes and creating assigned homework, client check-off (assignment-scoped RLS), session detail on both surfaces, the readiness panel. The extraction end-to-end run waits on ANTHROPIC_API_KEY (setup checklist item 2); the request builder and parse are unit-tested and every AI call rides the Ring 0 chokepoint (spend guard, ledger, refusal fallback, voice sweep).
- **Ring 3.5 (Practice Home): built.** `/today` composes the week across every client: sessions in the next seven days, homework awaiting review, digest and messages placeholders (Rings 6 and 5), and the three-week stall section (descriptive, never red-badged; threshold sits under CONFIRM 11). Practice members now land on /today from the root and the auth callback; practice mobile tabs are at the five-item max. No new tables, migrations, or env vars.
- **Ring 6 (Weekly digest): built.** Migration 0008 applied to the live project. The Friday cron (vercel.json, 22:00 UTC as the CONFIRM 6 proposal of Friday 3pm Pacific) gathers each active engagement's real week (sessions held, deliverables shipped, homework done, stage changes, what is scheduled next), refuses an empty week before any model call, drafts on the digest tier through the one AI chokepoint, voice-sweeps, and writes an inert ai_proposals row. The approval queue on /today is live: read the draft, approve and send (one branded email per client member; the record marks 'sent' only when every send succeeded) or dismiss. The end-to-end run waits on ANTHROPIC_API_KEY, RESEND_API_KEY, and CRON_SECRET (setup checklist).
- **Ring 5 (Messages): built.** Migration 0007 applied to the live project: one thread per engagement (unique constraint says so honestly), author-stamped messages whose insert policy demands self-authorship on the right side of the wall inside your own scope, bodies immutable to every session (column-level grant limits updates to read_at), no delete path. Client sends at /messages and the practice owners get a deep-linked email (targets via keystone_message_notify_targets, the minimal-disclosure definer RPC, since the pure-RLS client surface cannot read practice_members); the practice replies from the engagement page and the thread's client participants get one back. The /today messages card is live: unanswered threads with age. Email failure states are honest on both sides; the live send run waits on RESEND_API_KEY (setup checklist section 4).
- **Ring 4 (Deliverables and library): built.** Migration 0006 applied to the live project: deliverables (file or link, kind-constrained), resources (the practice-wide catalog, the documented no-client_id case), session_prep_resources (flagged spec addition; the join behind prep surfacing), two private storage buckets with path-scoped read policies and zero write policies. Practice ships from the engagement page (signed-upload direct-to-storage), the client watches the brass timeline at /deliverables and downloads through their own session (pure RLS end to end); resource authoring at /library/authoring (voice-swept), client read at /library; prep attaches on the run of show and surfaces above upcoming sessions and on session detail. Live matrix extended (storage stub added to the scratch Postgres); 78 gate assertions green. No new env vars.
- Engagement status: proposal out to SafeSpace, decision expected Thu Jul 9.
- **V2 program: landed.** The roadmap is `specs/keystone-v2.md` (five phases, each epic its own research, design, spec, build cycle); the engagement content is `docs/seed/keystone-safespace-seed.md`. Phase 0 (pilot and harvest) is in effect: no V2 feature build until the pilot yields signal; the SafeSpace seed is entered (see below); 1A (Practice admin UI) is the first V2 spec to write, during the pilot, per the V2 spec's closing note.
- **Phase 0 seed: entered on the live project** via `supabase/seed-safespace-pilot.sql` (idempotent; fresh installs get the same shape from the updated `seed.sql`). What went in: the engagement retitled to the proposal's "Systems and leaders: fundraising first" with the six-month end date; the workstreams renamed to the proposal's four (V1 gate 5 resolved); the July 7 working call and its thirteen-decision log as practice-visibility session notes; the six homework starters; the three readiness pillar notes; the charter draft and planned-deliverables ledger pinned as library resources; the nine SOBO resource starters. The section 12 exclusion wall now rides in the extraction prompt (`src/lib/extract.ts`, pinned by the engine spec) and is recorded in SECURITY.md section 4.

## V2 phase queue (specs/keystone-v2.md; each epic separately specced)

| Phase | Contents | Status |
|---|---|---|
| 0 | Pilot and harvest; seed first; spec 1A during | seed entered; pilot waits on SafeSpace's yes |
| 1 | Operability: 1A admin UI, 1B engagement builder, 1C one template, 1D first-run | 1A BUILT (2026-07-09; Remi approved the spec's four gates as recommended and pulled the build forward): migration 0009, `/settings/members`, designed invite emails, revocation through the predicates. 390px live run owed with the other rings; invite sends wait on RESEND_API_KEY. 1B BUILT (2026-07-09; gates approved as recommended): migration 0010 engagement_drafts, drafts list, the resumable builder, publish with invites through the 1A path. 1C (one real template) waits on the pilot per the V2 spec |
| 2 | The Charter spine: 5D approvals (pulled forward), 2A charter, 2B decision log, 2F workstream detail, 2D next moves, 2C outcomes, 2E Q&A, search | 5D and 2A BUILT (2026-07-10; gates approved as recommended): migration 0012, the charter editor, the client charter page with the sign-off block, the SafeSpace charter graduated to version 1 with its sign-off pending. 2B BUILT (2026-07-10; gates approved as recommended): migration 0013, the log on the engagement page, /decisions on the client surface, the thirteen July 7 decisions graduated to rows. 2F BUILT (2026-07-10; gates approved as recommended): migration 0014, the note form under each arc, the Home fold. 2D BUILT (2026-07-10; gates approved as recommended): the next-moves strip at the top of the client Home, no migration. 2C BUILT (2026-07-10; gates approved as recommended): migration 0015, the Outcomes section with the charter mirror and evidence links, /outcomes on the client surface, the eight seed doc outcomes graduated. PHASE 2 COMPLETE (2026-07-10): 5D approvals, 2A charter, 2B decision log, 2F workstream detail, 2D next moves, 2C outcomes, 2E Q&A, and engagement search, all built, gated, and matrix-proven. Live AI answers wait on ANTHROPIC_API_KEY. Phase 3 next: 3A editable AI review, then 4F notifications infra (pulled forward) |
| 3 | Delivery craft: 3C homework loop with the wall (pulled ahead on Remi's call 2026-07-10), 3A editable AI review, 4F notifications infra (pulled forward), 3B run of show, 3D deliverable lifecycle, 3E anchors, 3F library, 3G digest archive | 3C BUILT (2026-07-10; all five gates approved as recommended): migration 0017, the walled homework_activity trail, both surfaces, the manual add form, /today review card. Evidence file attachments are the agreed follow-up commit inside the epic (gate 3C-4). Reminders wait for 4F by design. 3H BUILT (2026-07-10, same day as its spec; gates approved as recommended): migration 0018, the date poll on both surfaces, candidates from the practice's own offered slots, confirm through the existing booking path. 3A BUILT (2026-07-10; all four gates approved as recommended, 3A-1 having shipped with 3C): migration 0019, the trigger-immutable payload, the editable review workspace, selective publish. 4F BUILT (2026-07-10, same day as its spec; all five gates approved as recommended): migration 0020, the recipient-walled notifications table, the lib/notify.ts chokepoint, eight emission points wired, the daily batched-email cron, the New-for-you blocks on /messages and /today, the mute on /account and Settings. Live sends wait on RESEND_API_KEY and CRON_SECRET like the digest. 3B BUILT (2026-07-10, same day as its spec; all five gates approved as recommended): migration 0021, the run of show on both surfaces, the sessions column grant closing the recon finding, reschedule notes with the homework date shift, the session reminder in the 4F batch. 3D BUILT (2026-07-10, same day as its spec; all five gates approved as recommended): migration 0022, About and session links on deliverables, append-only version history for files, deliberate acceptance riding the 5D approvals unchanged, inline View, both 4F emissions. 3E BUILT (2026-07-10, same day as its spec; all five gates approved as recommended): migration 0023, anchors sealed at send by the 0007 grant, server-derived labels, entry points on both surfaces, chips in both threads. 3G BUILT (2026-07-10, same day as its spec; all five gates approved as recommended): migration 0024, the sent-only client archive at /digests, the engagement page fold with the cadence select, cadence honored before any model call, digest anchors closing the 3E deferral. PHASE 3 IS COMPLETE: 3A, 3B, 3C, 3D, 3E, 3G, plus 4F pulled forward and the harvest-born 3H; 3F's library upgrade was delivered across the library commits. The 3C-4 evidence-file follow-up landed 2026-07-11 (migration 0030: the homework-evidence bucket with the coachee wall at the storage layer, the one storage-write policy, uploads pure RLS, matrix-proven), closing the epic's last open item. Per-recipient digest modes deferred to their own spec at tenant two |
| 4 | Practice OS: 4A action queue, 4B internal tasks, 4D readiness, 4C workload, 4E health, activity view, 4G pipeline (flagged), 4H knowledge base, 4I scheduling upgrade (harvest item 3) | 4A BUILT (2026-07-11, same day as its spec; all five gates approved as recommended): lib/actionQueue.ts, /today led by What-needs-you-today, no migration. 4F shipped early in Phase 3; the 4B kernel (the audience wall) shipped with 3C. 4D BUILT (2026-07-11, same day as its spec; all five gates approved as recommended): migration 0025, readiness_evidence behind the lens wall, the execution facts lib, evidence pickers per pillar, the share-as-reflection compose into the one thread. 4E BUILT (2026-07-11, same day as its spec; all five gates approved as recommended): lib/health.ts, the phrase on /engagements cards and the engagement page eyebrow, no migration. 4I BUILT (2026-07-11, same day as its spec, in a session parallel to this row; its migration renumbered to 0026_v2_scheduling_upgrade after 4D took 0025, sharing the 0026 number with ownership, flagged in the shipped entry): the scheduling upgrade, availability from the real calendar, durations, the invite on every calendar. PHASE 4 COMPLETE 2026-07-11 under the standing finish-Phase-4 instruction: 4B (split legible, no migration), 4C (0026 ownership + /team), activity view (0027 audit scope + the engagement fold), 4G (0028 pipeline behind the flag, SOBO off, per decided V2-5), 4H (0029 the knowledge-base audience wall on the one catalog). Fresh scratch matrix clean through 0029. Next: Phase 5 on Remi's word |
| 5 | Closeout: 5A closeout room, 5B portability, 5C case study, 5E scope and change orders | PHASE 5 COMPLETE 2026-07-11 under the standing move-to-Phase-5 instruction: 5A (0031, the room reads the record, six sections, publish plus 5D sign-off), 5B (built twice the same day on parallel branches and reconciled in the merge: the gated ZIP ARCHIVE from the agreement-review branch won, its four gates approved by Remi as recommended; lib/exportRecord.ts assembles the archive on the caller's session filtered to the shared shape, with the original files, the library, the message thread, and the published closeout inside; /export pure RLS on the client, /engagements/[id]/export on the practice, rate limited both sides, audited on the practice side only per the activity-view rule; no migration), 5C (0032, the fifth propose-then-accept job, quote by hand only, client approval on the record), 5E (0033, change orders with no fee column, structurally, per V2-6). Fresh scratch matrix clean through 0033. 5D shipped in Phase 2. V2 build complete; what remains is Remi's manual checklist and live 390px runs |

## V2 CONFIRM gates (specs/keystone-v2.md)

| # | Question | Status |
|---|---|---|
| V2-1 | Pilot length before Phase 1; Phase 1 specced during or after? | decided (Remi 2026-07-09, adopting the recommendations): three to four weeks of pilot before Phase 1 ships; 1A specced during the pilot |
| V2-2 | Templates: practice-private, or shareable across practices? (Schema now.) | open; standing recommendation for the 1C spec: practice-private rows (practice_id not null) with sharing later as an explicit copy operation, never shared rows, so the isolation matrix stays simple |
| V2-3 | Q&A: reads full consultant notes, or only client-published versions? | decided (Remi 2026-07-10, via gate 2E-1): the corpus is built by the ASKER'S OWN SESSION under RLS; client members get published and shared material only, practice members the fuller record; one wall, matrix-proven |
| V2-4 | Homework buyer-view wall on coachee revision history | decided (Remi 2026-07-09): yes, firmly. Founders see workstream-level completion only, never a coachee's per-item revision history; built into the read policy |
| V2-5 | Pipeline: product-tier, flagged off for SOBO | decided (Remi 2026-07-09): yes. Behind a practice-level feature flag SOBO leaves off; Trellis stays the business brain |
| V2-6 | Fee in-app (revisits V1 gate 9); change orders with or without a number | fee half decided (Remi 2026-07-09): in the charter, nowhere else (gate 9 above). The change-order-number half stays open for the 5E spec |

## Pilot harvest list (Phase 0's whole point; add items as the pilot surfaces them)

1. 2026-07-10, Remi (mid-3C): group scheduling. "Need a way for the scheduling to be something that all of them can agree on. The next date. I control what we have available, and then they pick times that work for each of them, and then we see what works." A coordination layer on top of Ring 2 booking: the practice controls the candidate slots (the availability windows already exist), each coached team member marks the times that work, the overlap surfaces the date everyone can make, then it books through the existing exclusion-constraint path. Disposition: SPECCED and BUILT same day as epic 3H (`specs/keystone-v2-group-scheduling.md`; all five gates approved as recommended).

3. 2026-07-11, Remi: the scheduling upgrade, Calendly's half plus Doodle's half. "I want the availability to come from my google calendar and i set up the boundaries, black out windows... then for my clients to be able to choose in a week which ones work for them... then I am able to see that and select the one that works for everyone and confirm it... it's created a Google Calendar invite that shows up on all of our calendars, with the zoom link from my zoom in there... they should be able to choose a 1, 1.5, or 2 hour session. Default 1 hour." Grounded on Ring 2 plus 3H, the reference read from the quarry's /meet system. Disposition: SPECCED and BUILT 2026-07-11 as epic 4I (`specs/keystone-v2-scheduling-upgrade.md`); gates 4I-1 through 4I-3 decided in the ask itself, 4I-4 through 4I-6 built on the recommendations per Remi's "get going with it," open for reword.

2. 2026-07-09, Remi: messages and notifications should be a deliberately built, first-class sidebar surface (messages plus nudges in one place), not a thread page plus loose emails. Disposition: folds into 3E (message anchors) and 4F (the notifications layer, already pulled forward to the front of Phase 3). The 4F spec gets a unified-inbox treatment: one in-app surface per side holding the thread, anchored context, reminders, and waiting-on state, with per-user channel and quiet-hours preferences and batching over pings. The single-calm-thread law from 3E holds: an inbox is a reading surface, never a second place to write. Disposition: SPECCED 2026-07-10 (`specs/keystone-v2-notifications.md`): the client inbox lives inside /messages, the practice's on /today; one batched daily email, every user a mute; quiet hours deferred with the recommendation until a real-time channel exists.

3. 2026-07-11, Susan (agreement review letter): "SafeSpace will retain full access to its data and the customized hub, including the ability to export all organizational data in a commonly used format, regardless of whether ongoing support services are continued." Real client signal that export is a trust condition, not a closeout ceremony, and the agreement's 30-day termination clause means handover must be possible from early in the engagement. Disposition: SPECCED and BUILT the same day as the pulled-forward 5B (`specs/keystone-v2-portability.md`, four gates approved by Remi as recommended); the parallel Phase 5 paper export was superseded by this gated archive in the merge. The engagement-record export is Keystone's half; the hub data export is BloomOS's half.

4. 2026-07-11, Remi: the client profile. "Each client on the practice side needs a profile: the steady state of that org in general, not just the current engagement: how much money is coming from them, where we're at, contact information." Diagnosed against the boundary: the money half collides with CONFIRM 9 (fee = charter only) and the Trellis boundary (cross-venture revenue is never productized here). Disposition: SPECCED and BUILT 2026-07-11 (`specs/keystone-v2-client-profiles.md`, all five gates approved as recommended). The profile at `/clients/[id]` gathers the org (roster as contact, every engagement with its health, record counts); the money half resolved at gate CP-2 as the engagement's own fee on the practice-only profile (a logged amendment to CONFIRM 9), with cross-venture revenue staying a Trellis pointer.

## FLAGS from the V2 landing (2026-07-09)

- The V2 spec draft arrived with ten em dashes (the top-ten list) and three uses of a banned word (the opens-the-door one, in the Phase 1 heading and twice in prose); the seed doc carried a banned word once (decision 5's used-deliberately verb). Landed with mechanical substitutions only: the dashes became colons, and the two words became "opens" and "used" (the seed doc's own section 3 wording). No other content touched; Remi may reword.
- The July 7 working call's hour is not in the record; the seeded session says noon Pacific as a placeholder. Remi 2026-07-09: stands as is, no correction needed.
- The decision log is published ('shared') as of the fee gate decision: decision 11 now says "accepted as proposed" and points at the charter, which is the one place the $25,000 appears (gate 9). The original numbered wording is preserved in this repo's git history only.
- V1 `deliverables` holds shipped artifacts only (kind file or link), so the PLANNED ledger from seed doc section 8 lives as a pinned library resource until V2 3D (deliverable lifecycle) gives plans a first-class home. RESOLVED 2026-07-13: migration 0035 (planned deliverables) is that home; the ledger's sixteen items are planned rows on their workstreams and the library placeholder is deleted (seed-planned-deliverables.sql).
- The seed doc section 5 says the readiness markers are "already in Keystone"; the live table had zero rows (the panel existed, the content did not). The three pillar notes are now seeded from section 5.
- The library is practice-wide in V1, so the charter draft and ledger resources are technically visible to any future client of the practice. Acceptable with one client; V2 3F adds client-specific visibility before tenant two's client sees a library. UPDATE 2026-07-11: the charter placeholder self-retired when 2A graduated it; the generic sweep (docs/library-generic-sweep.md) rewrote the nine guides that carried SafeSpace material, so the SafeSpace ledger is now the ONE client-named row on the shared shelf. Before a real second client's members arrive it needs a first-class planned-deliverables home, per-client assignment (the 3F deferral, shaped as a resource_assignments join, never a client_id column), or retirement into the engagement record.

## Manual steps

Every operational step (env vars, API setups, dashboard switches) lives
in **`docs/setup-checklist.md`**, maintained per ring and done in one
sweep after the full build. Note: branch previews build green on every
push but sit behind Vercel SSO (open them while logged into Vercel; the
build session's sandbox cannot reach vercel.app hosts, so its visual
runs were against the identical commit locally).

## The ring queue

| Ring | Contents | Status |
|---|---|---|
| 0 | Preflight: repo, platform layer, tokens, docs, agents, gate scaffolds, preflight doc | done |
| 1 | The spine: practices, clients, members, email-keyed invites, engagements, workstreams with the parallel arc, stage events, permission authority, RLS, the seeded cross-practice and cross-client isolation matrix, sidebar shell both surfaces, client progress view, login page | built; live DB seeded; 390px live run owed |
| 2 | Sessions and scheduling: availability windows, slot picking, Google Calendar OAuth, tz-correct sync, reschedule | built; Google end-to-end run owed (needs env) |
| 3 | Notes and homework: paste transcript, AI proposes (inert), consultant accepts, client checks off, review queue, session detail, readiness panel | built; live AI run owed (needs env) |
| 3.5 | Practice Home, the Monday screen | built |
| 4 | Deliverables and library | built |
| 5 | Messages plus Resend notifications | built; live email run owed (needs RESEND_API_KEY) |
| 6 | The weekly digest (cron, approval queue, refuses an empty week) | built; live run owed (needs env) |

Then stop and run SafeSpace on it for two weeks before any Ring 7 talk.

## CONFIRM gates (nothing ships assumed)

The spec numbers these 1 through 12 and 14; there is no gate 13 in the spec (flagged in the preflight doc).

| # | Question | Status |
|---|---|---|
| 1 | Domain: app.soboconsulting.com, or a keystone domain from day one? | decided: app.soboconsulting.com (DNS CNAMEd to Vercel 2026-07-09; attach the domain to the Vercel project, checklist section 1) |
| 2 | SafeSpace logins: susan@, liesl@, aris@, jasmine@ (all safespace.org); confirm the four and whether anyone else joins | decided: the four confirmed by Remi 2026-07-09, exactly as seeded; nobody else joins for now |
| 3 | Library access after the engagement ends: keeps or lapses? | open |
| 4 | Shannon: practice login in v1? | decided: yes (Remi 2026-07-09); shannon@ambitionangels.org, consultant |
| 5 | SafeSpace workstream names: confirm or rename with the client's language (spec lists five seeds but the gate says "the four seeded above"; flagged) | decided: the client-approved proposal names four (seed doc section 13); renamed in the live DB 2026-07-09 via seed-safespace-pilot.sql |
| 6 | Digest day and hour (proposal: Friday 3pm Pacific) | open |
| 7 | Name clearance: trademark plus domain check on "Keystone" before public use | open |
| 8 | Session locations: video link source (Meet from the calendar event, or Zoom)? | decided (Remi 2026-07-11, in the 4I ask): Zoom, the personal room link pasted once into scheduling settings and snapshotted onto each session; the Zoom API stays a later epic |
| 9 | Fee visibility: does the engagement show the $25,000 anywhere in-app, or never? | decided (Remi 2026-07-09), AMENDED (Remi 2026-07-11, gate CP-2): the fee shows in the charter AND on the practice-side client profile (`/clients/[id]`), nowhere else. The amendment is safe because the profile is practice-only (no client ever sees it) and shows the engagement's own fee_display, never a cross-engagement or cross-venture total; the client-facing surfaces are unchanged (charter still the only place a client meets the fee). fee_display set; the charter resource carries the fee line; the shared decision log names the fee by reference only |
| 10 | Liesl's posture: full login plus digest, or digest-first given the advisory move? | open |
| 11 | Stall threshold: three weeks proposed; twice-weekly month-one cadence may want two | open |
| 12 | Readiness notes: consultant-only forever, or shareable per note as a deliberate act? | open |
| 14 | Nav label on soboconsulting.com: "Client Login" until gate 7 clears | open |

## Blocked

- The live 390px data run: blocked on manual step 1 above (auth redirect allow-list), then any seeded email can sign in with a magic link.
- Google sign-in: code is live on the login page; the door opens after checklist section 3b (dedicated sign-in OAuth client) plus the Supabase provider switch in section 1. Until then the button shows its honest could-not-start state.
- Calendar end-to-end: blocked on manual step 2 (Google OAuth creds + token secret).
- Domain wiring: CONFIRM 1 decided (app.soboconsulting.com). Code and vercel.json now carry the domain; the remaining step is attaching it to the Vercel project (checklist section 1) and updating the Supabase allow-list and Google redirect URIs to match.
- Invite sends: unblocked (CONFIRM 2 decided; the four seeded SafeSpace emails are confirmed as-is). The four can sign in as soon as the auth allow-list step lands.

## Recently shipped

- 2026-07-17: ISSUE REPORTS GO OWNER-ONLY-READ, TEAM-CAN-FILE (migration 0037, on Remi's ask "I want database. Only Remi sees the reports; practice leaders can report too and it comes to me"). Read narrows from both walls to the owner alone: a new issue.read permission granted to owner only, the read policy asks keystone_can(practice_id, null, 'issue.read'), so a consultant or a client member now reads ZERO issue_reports at the database, not just in the app (the app-layer-only FLAG from the triage-screen entry below is now closed). Reporting is opened the other way: the report FAB mounts on the practice surface too (src/components/PracticeReportFab.tsx, report only, no coach since there is no single engagement there), so a consultant or the owner files a system issue that comes to the owner. A practice-authored report has no client and no engagement, so engagement_id and client_id are nullable now, with a CHECK (issue_reports_scope_shape) that keeps a client-authored report fully scoped (both ids present) so the cross-client wall on client reports stays structural. The practice report action (src/app/(practice)/report/actions.ts) inserts on the session under RLS and emails the owner through a new minimal-disclosure RPC keystone_issue_notify_targets(p_practice) (the practice-caller twin of keystone_message_notify_targets, membership-checked, pinned search_path, owner emails only, revoked from anon). The /issues screen now labels practice-authored rows "From your team" and still reads on the owner's session. The isolation matrix and e2e/issue-reports-isolation.spec.ts rewrote to the new model (a client leader and a consultant each file but read zero; only the owner reads; the cross-client and cross-practice walls hold; a filed report is a record). Verified: 311 static gates green, the full RLS matrix passes against Postgres 16 with 0037 applied, typecheck, lint, and build clean. Migration 0037 not yet applied to the live keystone project (owed with the other live steps).

- 2026-07-17: THE REPORTED-ISSUES TRIAGE SCREEN, a follow-up on Remi's ask ("a screen just for me to see what happens when anyone puts in an issue"). Owner only: practiceNav grew an Issues item that appears only when the resolved role is owner (desktop rail only, so the practice mobile bar keeps its five), and the page at /issues (src/app/(practice)/issues/page.tsx) sends any non-owner back to /today. It reads issue_reports on the owner's own session under RLS (a practice owner sees every report of their practice and nothing of any other), newest first, each row showing the kind, the body, the client, and a link into the engagement. It reads the rows, never edits them (the table is immutable by policy; a status or resolve workflow is a later gated change if the pilot asks). One honest limit flagged, not hidden: the RLS read policy still admits any practice member, so the owner-only wall here is at the app layer; a data-layer owner-only lock is a later change if wanted. e2e/issue-reports-isolation.spec.ts grew the owner-gate and owner-only-nav assertions; the new Flag icon is in the Sidebar map. RESEND_API_KEY is now flagged on docs/setup-checklist.md as what the per-report email needs (until set, the report saves and shows on this screen with no email, degrading honestly). Verified: 307 static gates green, typecheck, lint, and build clean.

- 2026-07-17: THE HELP FAB BUILT, a harvest-born epic on Remi's ask to bring the BloomOS floating button (an AI assistant plus a report-an-issue flow) to Keystone. Spec `specs/keystone-v2-help-fab.md`; four scope decisions defaulted as recommended (client surface first, a dedicated table plus an email per report, a simple form over the BloomOS AI intake interview, a chat-style display over the grounded single-shot coach), each reversible and flagged for reword. The diagnosis that shaped it: Reed's twin already exists. The 2E engagement Q&A (lib/qa.ts, lib/qaCorpus.ts, askQuestion) answers only from the engagement's own record, cites sources, and refuses when the record is silent, so the coach half reuses it unchanged with no new AI plumbing, no new table, and no new rule. The only net-new storage is the report. Migration 0036: issue_reports, both scope ids on the row, kind (bug, confusing, idea) and body checks, reported_side naming the wall, an issue.write permission granted to client_member, owner, and consultant. Read admits both walls in scope; insert demands self-authorship on the caller's own wall through keystone_can; no update and no delete policy, so a filed report is a record and triage reads it, never edits the client's words. The report action (src/app/(client)/report/actions.ts) is pure RLS like the Ring 5 message send: resolve the active engagement on the session, rate-limit (ISSUE_REPORTS_PER_MIN and _PER_HOUR, new in lib/rateLimit.ts), insert on the session client, then email the practice owners with targets from keystone_message_notify_targets (reused, so the surface never reads practice_members) with the honest email degrade preserved. The FAB (src/components/HelpFab.tsx, the first modal primitive in the codebase, mounted only in the client layout) is a forest circular button above the 390px bottom tab bar that opens a two-tab window (Coach, Report an issue) on the frozen tokens with a brass tick on the active tab, Escape and scrim to close, focus into the panel on open. The per-feature gate met in the same PR: the isolation matrix block in isolation-seed.sql and e2e/issue-reports-isolation.spec.ts cover both scope walls, the self-authorship wall, the cross-client forge refusal, immutability, and the surface's pure-RLS and rate-limit contract. Verified: 305 static gates green, the full cross-practice and cross-client RLS matrix passes against Postgres 16 with 0036 applied, typecheck, lint, and build clean. The 390px live run is owed with the other rings (blocked on the auth allow-list like every surface). Deliberately parked as named follow-ups: a screenshot attachment on a report, the BloomOS-style AI intake interview, a practice-side triage list view (the read policy already admits it; email is the delivery for now), and the practice-side mount of the FAB.

- 2026-07-13: PLANNED DELIVERABLES BUILT AND APPLIED LIVE, on Remi's ask, closing the promises-need-structure deferral. Migration 0035: a status column ('planned'|'shipped', default 'shipped' so every existing row keeps its meaning), kind and delivered_on nullable, and ONE two-sided check (deliverables_status_shape) replacing the 0006 kind and payload constraints: a shipped row must carry its artifact and date, a planned row can carry neither. NO RLS changes; both walls have been on the table since 0006 and planned rows are client-visible by design (the plan is part of what the fee buys). The write side: planDeliverable (title, workstream, expected note, About; audited), and createDeliverable grew a fulfill path that flips the SAME planned row to shipped (one record, promise to receipt; the deliverable_shipped notify and the audit fire at the flip, action deliverable.fulfilled). Acceptance requests refuse planned rows. Surfaces: the practice engagement page splits Planned (dashed cards, remove, fulfill via the ship form's Fulfills select) from shipped, with a Plan-a-deliverable form; the client /deliverables timeline leads with a quiet Coming section and keeps the brass timeline for receipts. Every record-shaped read filters to shipped, pinned by the gate: qaCorpus, recordSearch, exportRecord, the digest gather, case-study facts, both closeout ledgers, the client-profile shipped count, and the client home latest-deliverable cards (the nulls-first ordering trap the survey caught). THE LEDGER GRADUATED: seed-planned-deliverables.sql turned the seed doc section 8 items into sixteen planned rows on their four workstreams and deleted the library placeholder, retiring the last client-named row from the practice-wide shelf (the sweep FLAG below); the pilot seed gained the never-comes-back guard (the charter pattern). e2e/planned-deliverables.spec.ts pins the shape, the same-row flip, the shipped-only acceptance, the read filters, and the graduation; 296 gate assertions green, fresh scratch matrix clean through 0035, typecheck, lint, and build clean. Live verified: 16 planned rows, zero shape violations, the ledger resource gone. The 4A deliverable-promised queue line is now buildable and deliberately unbuilt.

- 2026-07-11: THE LIBRARY GENERIC SWEEP, on Remi's ask after the design question (is the library SafeSpace's or every client's?). The decision: the catalog stays practice-wide per spec 5.1 and reads as SOBO IP for any nonprofit; client-specific material belongs in the client's walled engagement record; per-client assignment waits for a real second client (and would be a resource_assignments join, never a client_id column on resources). The audit found nine of twelve client-audience guides carrying SafeSpace material: the client's actual messaging angles (name, Youth Action Board, the peninsula framings), its donor-base size (the 1,200 contacts), a program name (Campus) in an example, Susan's too-early multi-year ask reading as the practice's own story, and youth-specific illustrations throughout. `supabase/seed-library-generic-sweep.sql` rewrites the nine bodies, layered after guides and additions per the additions precedent (checked-in history untouched, whole-body updates by title, idempotent); every removed detail already lives in the engagement record or the seed doc, so nothing moved, only the shared copy changed. Deliberately not swept: the SafeSpace ledger, the one client-named row, tracked in the extended library FLAG above. Full audit table and the live-apply clobber check in `docs/library-generic-sweep.md`.

- 2026-07-11: THE CLIENT PROFILE BUILT, a harvest-born epic on Remi's ask ("each client on the practice side needs a profile: the org in general, not just the current engagement"). Spec `specs/keystone-v2-client-profiles.md`, all five gates approved as recommended. The spec's own section 5 architecture sketch reserved `/clients/[id]` from day one and it was never built until now. Migration 0034: a PRACTICE-ONLY `client_profiles` table (relationship_note, primary_contact_member_id into the roster, website, relationship_started_on; both scope ids, client_id unique, one row per client), read by is_practice_member alone and written by owners only (practice.manage). NOT columns on clients: code review caught the first cut doing exactly that, and because clients_read admits the client's own member and RLS is row-level, the private relationship note would have been readable by that client member straight from the REST endpoint even though no client-surface code selects it; moved to its own table so the wall is RLS, not app-code omission (the 1B/engagement_drafts precedent). The live matrix now proves the client's own member reads ZERO profile rows. The profile (`/clients/[id]`, practice-only, reads on the caller's session under RLS, a clean 404 out of scope): the header and relationship note, an at-a-glance strip, the roster as CONTACT with the primary contact marked, every engagement (active and past) leading with its health phrase and 4E fact lines plus arcs, dates, and fee, and the record-so-far counts (sessions held, deliverables shipped, decisions logged, messages). `/clients` upgraded: each row links into the profile and carries the health phrase per active engagement. The composition factored into `src/lib/healthInputs.ts` (assembleHealth) and the /engagements list refactored onto it, so momentum reads identically on both surfaces and cannot drift (gate CP-1). THE MONEY DECISION (gate CP-2, logged not silent): the profile shows the engagement's own fee_display, a deliberate amendment of CONFIRM 9 from "charter only" to "charter and the practice-side profile"; safe because the profile is practice-only and the fee is a single engagement's, never a computed cross-engagement or cross-venture total (the gate greps for summation); cross-venture revenue stays a Trellis pointer per spec section 2. The owner edit action verifies the client against the caller's practice and a chosen primary contact against that client's roster before writing, and voice-sweeps the relationship note. The isolation matrix grew the profile block (owner creates and reads the row, a consultant reads but the write returns zero, the client's own member reads zero and cannot write, member_a2 reads zero cross-client, owner_b reads zero cross-practice); `e2e/client-profile.spec.ts` pins the practice-only table shape (is_practice_member read, never is_member_of_client), the owner-only upsert, the single-fee boundary, the client surface never touching the table, and the shared assembly. 290 gate assertions green; the live matrix passes on scratch Postgres 16; typecheck, lint, build clean. Merged to main as PR #1 (reconciled with the parallel add-a-client and signed-agreement lines). MIGRATION 0034 APPLIED LIVE to the keystone project: client_profiles verified with RLS on and its three policies, and the security advisor is clean (the table trips no lint; the pre-existing deny-all and definer-function advisories are unchanged). 390px live run owed with the other rings (the auth allow-list blocks it like every surface).
- 2026-07-15: SIGNED AGREEMENT ALIGNMENT. The executed Consulting Services Agreement (effective Jul 15, 2026, board approved Jul 13) is now the source of truth and supersedes the Jul 11 redline at `docs/seed/safespace-agreement-v2-draft.md`. It differs from the draft in four places, and the SafeSpace content now follows the signed version everywhere it lives: the weekly time is four to six hours per coachee per week, six to eight in month one (not five to eight); the board engagement is up to two working sessions (not one); the term runs Jul 10, 2026 through Jan 10, 2027; and a hosting and continuation section is added (first year included in the fee, then Bloom Grow at $250 a month held five years from the end of the included year, cancel any time, full export and help moving if SafeSpace leaves). Updated: the seed doc (§2, §3 charter body and a new hosting subsection, §8 ledger, decisions 14 and 17 corrected, decision 18 added, §13 note), the pilot seed SQL charter (time, board sessions, hosting), and the redline doc header and body reconciled to the signed version. The sobo-consulting proposal was updated in the same pass on its own branch. Decisions 17 and 18 in the seed doc carry the detail. Supersedes the Jul 11 "decided at five to eight" note below.

- 2026-07-11: THE PARALLEL LINES MERGED, in two passes because main kept moving (first the phase 4 and 5 completion run, then the 4I scheduling upgrade with the Ambition Angels demo seed). One session ran phases 4 and 5 to completion under the standing instructions (entries below), another built 4I and the demo seed, and the agreement-review session processed Susan's letter and built 5B from its own spec with Remi approving the four gates in conversation. Two of the lines built 5B. The merge kept the GATED ARCHIVE (zip on the caller's session, shared shape on both sides, original files, library, messages, rate limits, practice-side-only audit per the activity-view rule) and folded the other line's published closeout into it as closeout.md, so the Phase 5 gate (e2e/export-record.spec.ts) and the archive gate (e2e/export-gate.spec.ts) both hold against one lib. The client route lives at /export (the closeout room links it); the account page's Your-record card and the engagement page's Export-the-record link both point at the one implementation. The parallel paper build (single markdown, no files) is superseded and its spec text replaced by the gated one.

- 2026-07-11: 5B BUILT on the agreement-review branch, same day as its spec (all four gates approved by Remi as recommended: library resources in the client archive, zip over PDF, no practice-only appendix, messages included). No migration. lib/exportRecord.ts assembles the archive on the CALLER'S OWN SESSION under RLS (the 2E corpus discipline) with every query filtered to the SHARED shape (published and superseded charters, shared notes never touching transcript columns, client-audience homework, sent digests, client-visible documents, and post-merge the published closeout), so both sides export the same client-shaped zip and a forwarded archive can never leak practice-only material; homework activity threads ship only in a client caller's own archive, where the 3C wall admits their own rows alone. The zip (fflate, the one new dependency) holds README.md with honest counts and a never-in-an-export section, one markdown document per artifact kind, and the original files streamed through the session storage policies, with failed fetches LISTED in the README rather than silently dropped and a 150MB ceiling that refuses honestly. Surfaces: "Your record" with Download on client /account (pure RLS end to end), "Export the record" beside the charter line on the practice engagement page. Both routes rate limited (EXPORT_PER_HOUR 3, EXPORT_PER_DAY 8) and audited metadata-only; SECURITY.md carries the 5B paragraph. e2e/export-gate.spec.ts pins the walls and unit-tests the renderers. The live download run is owed with the other live runs (auth allow-list, checklist section 1).

- 2026-07-11: the agreement review's open ends closed on Remi's answers. The weekly time expectation is DECIDED at five to eight hours per coachee per week, like training (decision 17 in the seed doc; proposal, charter body, pilot seed, and redline all updated). The redline's installment dates and hub security description are confirmed and their brackets removed; the one remaining bracket is the board approval date in section 12, open until the disinterested board members vote. Still Remi's, in-app: publish charter v2 from the seed doc section 3 body (withdraws v1's pending sign-off per gate 2A-2) and update the live ledger resource via /library/authoring.

- 2026-07-11: Susan's agreement review processed (her Jul 11 letter against the proposal and Remi's draft Consulting Services Agreement). Decisions made by Remi the same day: the board fundraising engagement comes IN SCOPE, WALLED (toolkit, introductions playbook, and a board working session as deliverables; board-member-level intelligence only in the hub CRM, entered by SafeSpace; the seed doc section 12 board bullet gained the precision edit distinguishing governance material, which stays out permanently, from board-fundraising work product), and the agreement keeps the legal name once with "Sobo Consulting" as the defined short form. Content amended: the sobo-consulting proposal (fundraising deliverables named in full with a major donor component, the platform ownership/export/security line, the weekly time range, the board component, success measures pointed at the shared room, Alameda County removed), the seed doc (charter section 3 body updated for charter v2, ledger section 8 grew five items, decisions 14 through 17 appended, section 13 open items), and the pilot seed SQL kept in sync for fresh installs. New: docs/seed/safespace-agreement-v2-draft.md, the full agreement redline with a changelog; the one open bracket is the board approval date.

- 2026-07-11: the Ambition Angels demo engagement SEEDED on the live project (`supabase/seed-ambitionangels-demo.sql`, idempotent), on Remi's ask: a second engagement, fabricated on purpose and rooted in what is real (the shipped ambitionangels.org redesign, the BloomOS build actually underway), so the practice can log in from the client side and see the full system mid-flight. The fiction: a six-month, $25,000 engagement (fee in the charter only, gate 9), March 9 to September 9, month 4 of 6 today. What went in: the client and two client members (shannonsfair@gmail.com and remisobo@gmail.com; their auth users pre-created mirroring the live GoTrue row shape and memberships claimed, so a magic link signs straight into the seeded history), four workstreams with eleven stage events (the website arc done, BloomOS in train, data in build, handoff in design), sixteen held sessions plus three booked with run of show on the newest three, five session notes (one practice-only), the published charter with Shannon's signed approval, ten session-linked decisions, ten homework items (a full submit, send-back, accept loop; a submission awaiting review; an honest block; one internal practice task behind the audience wall), seven link deliverables (the live site accepted via 5D; the board view acceptance pending, so Your-next-moves has a real ask), five outcomes with six evidence links, the three readiness pillars with receipts, workstream notes, a ten-message thread (one anchored ask, the newest client message unanswered so the action queue speaks), ten sent digests in the archive, six notifications, and prep on the July 16 session. The practice now has a second client in production data, so every cross-client wall has a live case. FLAGS: deliverable URLs other than ambitionangels.org are doc-shaped placeholders (they resolve to a permission wall, which reads as real); no agreement PDF is seeded, so the client home shows the quiet empty state until one is uploaded; removal is one delete of the 'Ambition Angels' client row (everything scoped cascades) plus the two auth users by email.

- 2026-07-11: post-close review sweep SHIPPED. An adversarial review of the whole 4E-through-Phase-5 batch surfaced one high finding and three worth fixing, all closed the same day. (1) The re-consent law: a case study's approval covers the TEXT the client read, so every content write (accept or save) now withdraws a pending ask and drops status back to draft; an edited-after-approval study says "the approval on record covers the OLD text only" and must be re-asked, an unchanged approved study cannot be re-asked, and the client stops seeing the row the moment it is edited (RLS on the draft status). (2) The closeout's honesty stamp: post-publish edits now surface on both rooms ("sections last edited ...") and the client sign-off block says plainly when the sections changed after the signature. (3) Matrix depth: closeouts, case_studies, and change_orders now carry the same-practice DIFFERENT-client case (member_a2 reads zero of client_a1's rows), not just the conflated cross-practice case. (4) Owner writes verify the engagement against the caller's practice explicitly instead of no-op-ing through RLS and misreporting saved. One noted-not-fixed edge: the /engagements and /team health reads use practice-wide top-N fetches (500 messages, 1000 events) filtered in memory; at much larger practice scale an engagement's tail could fall outside the window and read falsely quiet; ticket-worthy at tenant two, harmless at SOBO scale. 261 gate assertions green; fresh matrix clean.

- 2026-07-11: PHASE 5 BUILT AND THE V2 PROGRAM CLOSED, four epics in one run under the standing move-to-Phase-5 instruction, each with its spec, gate, and matrix cases; 260 gate assertions green and a fresh scratch matrix clean through 0033. 5A the closeout room (migration 0031): one row per engagement holding ONLY the six authored sections (what-to-do-if-it-breaks first, ownership, maintenance, training, risks, what-next); the room READS the live record (charter, outcomes, deliverables with acceptance, arcs against the final stage, last digest), never copies it; drafts invisible by policy, publish is the deliberate moment with the 4F touch, the sign-off rides 5D approvals unchanged, no delete (you do not un-ring the bell); /closeout in the client desktop nav. 5B portability (no migration): lib/exportRecord.ts builds the whole record as one markdown file on the CALLER'S session, filtered to published-and-sent shapes so both sides export the same paper; /export pure RLS on the client ("it is yours"), /engagements/[id]/export on the practice. 5C the case study builder (migration 0032): the FIFTH propose-then-accept job, same architecture (lib/caseStudy.ts is the digest engine's twin; the draft lands inert in ai_proposals; one accept; sonnet tier; rate-limited and spend-capped); the model never writes the client quote (captured by hand); the client reads it only at review and approves through 5D; nothing public without that record. 5E change orders (migration 0033): the ask and the answer on one shared page, structurally no fee column per V2-6; the client asks from /charter ("Outside the lines"), only the practice decides and the answer is required in writing; nobody deletes a boundary held; 4F kinds both directions. CLAUDE.md AI rules updated to five jobs.

- 2026-07-11: 3C-4 BUILT, evidence files on the homework trail, closing Phase 3's last open item (the follow-up gate 3C-4 agreed inside the epic). Migration 0030 applied live: file columns on homework_activity and the homework-evidence bucket with the V2-4 wall at the STORAGE layer: the practice reads by path, the assigned coachee through their own trail row, the teammate/buyer persona gets nothing at either layer (matrix-proven from every seat, including forged uploads under someone else's item). Uploads are the system's one storage-write policy because the client surface is pure RLS: the coachee inserts under their own OPEN item's exact scope path, nothing else; no update or delete policies anywhere (append-only like the trail). The submit form takes a file (10 MB, document and image shapes; a submission may now be words, a link, or a file); attachments render in both threads and stream through session-client download routes on both sides. The matrix stub now grants storage inserts to authenticated the way real Supabase does, so upload walls are testable. 244 gate assertions green; fresh scratch matrix clean through 0030.

- 2026-07-11: 4H BUILT and PHASE 4 CLOSED, the knowledge base container, under the standing finish-Phase-4 instruction. Migration 0029 applied live: resources.audience ('client'|'practice', default 'client' so every existing row keeps its meaning), the kind list grown with the knowledge-base shapes (sop, agenda_template, homework_template, deliverable_template, prompt_recipe, diagnostic), and the read policy narrowed: practice reads everything, a client session reads client-audience rows only (the homework audience pattern applied to the catalog). Authoring now shows two shelves (Client learning path / Knowledge base) with the audience choice on both forms; the client library page is byte-identical in code because the policy is the wall. Container only: no SOBO playbooks moved in; whether they live here or stay in Trellis is Remi's deliberate call later. 240 gate assertions green; fresh scratch matrix clean through 0029 (all migrations in filename order, the deals and audience walls proven from every seat).

- 2026-07-11: 4G BUILT, pipeline-lite behind the flag, under the standing finish-Phase-4 instruction and the decided V2-5. Migration 0028 applied live: practices.pipeline_enabled default FALSE (SOBO stays off) and deals, practice-only (no client_id: there is no client yet), stages lead through converted, a link to the draft it became, and structurally NO money columns (the gate greps the DDL): Trellis stays the business brain because this table has nowhere to put a number. RLS is the drafts discipline (practice read, engagement.write writes, no delete: closed is a stage) with the four matrix cases seeded (client member zero rows and denied write, owner_b zero and no cross-practice move, nobody deletes). /pipeline exists as a route, NOT in the nav; with the flag off it says so honestly and points at Trellis, and every action re-reads the flag server-side and refuses (fail closed; page copy is not enforcement). Convert follows a verbal yes only and lands in the BUILDER as a draft titled after the deal, the deal stamped converted with the receipt. 235 gate assertions green.

- 2026-07-11: activity view BUILT (the roadmap's addition item 5), under the standing finish-Phase-4 instruction. Migration 0027 applied live: nullable practice_id and engagement_id on audit_log, NO foreign keys (an audit row outlives whatever it describes, untouched even by a cascade), feed index, table stays deny-all. logAuditAction accepts the scope and all 28 engagement-scoped call sites (engagement actions, session actions, charter actions) now stamp it; older rows carry null scope and simply do not appear, the trail honest about when it started. The engagement page gains a quiet Activity fold at the bottom: action, when, who, through listEngagementAudit behind the practice check; detail payloads are never rendered even though they are metadata. Gate pins no-FK/no-policy, per-file stamp counts, the payload-free fold, and no client import of lib/audit. 230 gate assertions green.

- 2026-07-11: 4C BUILT, staff workload and ownership, under the standing finish-Phase-4 instruction. Migration 0026 applied live: owner_practice_member_id on engagements and workstreams, columns only, riding the standing walls (updates through keystone_can; a client session cannot resolve the reference and the gate proves no client file reads the column). Owner pickers live where the work lives: one beside the engagement header, one per workstream row, both verifying the assignee against the caller's own practice before writing. /team is in the nav: one card per active practice member (engagements owned, workstreams owned with stage, the next seven days of sessions through engagement ownership, and the waiting-on facts inside their rooms as prose counts), plus the No-owner-yet card, because unowned work is the first workload fact. No capacity, no utilization, no ranking; the gate greps for all of it. 226 gate assertions green.

- 2026-07-11: 4B BUILT, internal tasks vs client homework, under Remi's standing finish-Phase-4 instruction (gates taken as recommended; no migration, the 0017 wall and 3A birth already stood). The three kinds of work now read as three kinds: the practice engagement page splits Open (client homework, with practice-assigned items marked "our commitment") from Internal tasks (practice assignee, due, one-click Done and Reopen; check-offs with no trail rows and no notifications, because we do not ping ourselves about ourselves); the client homework page stops rendering our commitments as "unassigned" team items and gives them their own read-only strip, "With your consultant team: ours to deliver, on the record so you can hold us to it." e2e/internal-tasks.spec.ts pins the wall text unchanged (and that no later migration widens the audience enum), the silent completes, and the form-free client strip. 222 gate assertions green.

- 2026-07-11: 4I BUILT, the scheduling upgrade, spec and build the same day (harvest item 3; gates 4I-1 through 4I-3 were decided in Remi's ask, 4I-4 through 4I-6 built on the recommendations and stay open for reword). FLAG, then the renumber: this epic built in a session parallel to the Phase 4 and 5 run, and 4D took migration number 0025 on the live project mid-build; the scheduling migration ships as 0026_v2_scheduling_upgrade, nothing overwritten. The parallel run later minted 0026_v2_ownership, so two files share the 0026 number; they touch disjoint objects and lexical order keeps the apply deterministic (ownership first). Migration 0026_v2_scheduling_upgrade applied live: scheduling_settings (buffer, notice, horizon, the 60/90/120 offer with the universe pinned by CHECK, the video link; both sides read because the pure-RLS booking page needs the boundaries, practice writes), scheduling_blackouts (practice-only rows; clients meet blackout time only as anonymous busy intervals), calendar_busy (deny-all, the google_connections precedent), busy_pulled_at on the connection, and keystone_busy_intervals widened to union sessions, the cached real calendar, and blackouts behind the same membership check, bare shape, and 60 day cap. The engine gained the buffer; assembly reads the settings and validates durations (resolveDuration is pure and gate-tested; an agreed length rides exactDurationMinutes so a narrowed offer never rewrites a poll or a reschedule). The pull is the new half of sync (pullBusyForMember inside syncMember): on the Settings button, after the OAuth callback, and hourly via the secret-gated /api/calendar/refresh cron (vercel.json), with the last pull stated in Settings. The invite is real: attendees are the consultant plus the client team (Google notifies all on insert, patch, and delete), the video link snapshots onto sessions.location and rides as the event's Where, practice-side bookings push immediately, and client-side moves fire the HMAC-gated /api/calendar/push (lib/pushToken.ts, KEYSTONE_TOKEN_SECRET only, rate limited, canonical origin) with the cron as backstop; a Google failure never fails a booking. Surfaces: the boundaries and blackouts cards on Settings, the duration line on client booking and poll creation (one duration per poll), and the day-grouped poll card. CONFIRM 8 closed: Zoom personal room link, the Zoom API a later epic. Code-reviewed (four findings, all fixed: canonical push origin over host headers, the duration CHECK, the consultant on the attendee list, doc names matching code). all gate assertions green on the fully merged tree (the count in the Phase 5 entry supersedes); fresh scratch matrix clean across every migration; typecheck, lint, build clean. The live Google end-to-end still waits on checklist section 2 (OAuth creds plus KEYSTONE_TOKEN_SECRET), now covering free/busy pull and attendee invites as well as push; CRON_SECRET gates the hourly refresh like the other crons.

- 2026-07-11: 4E BUILT, engagement health in voice, spec and build the same day (all five gates approved as recommended). No migration, as gated: health is DERIVED at render, never stored; a stored label trail would be a performance file by another name. lib/health.ts holds the deterministic ladder (ready for closeout, waiting on us, waiting on the CLIENT never a member, moving, holding steady, quiet for N weeks; first match wins) plus the two shared derivations: replyLag (the newest message per thread decides who owes, oldest standing age per side) and reviewStanding (the homework trail decides what is submitted-and-waiting versus overdue-unsubmitted). Fact lines in prose under the phrase: last session age, homework on time this month, digest consistency against the cadence, and the replies line that renders BOTH WAYS or not at all (their lag never shows without ours beside it). /engagements cards lead with the phrase and one fact line (arcs untouched); the engagement page carries the phrase quietly in the eyebrow with the facts in a plain paragraph under the header. Practice surfaces only; e2e/health.spec.ts walks every rung, proves first-match-wins, greps the vocabulary (no score, percent, grade, red, or behind), and pins no-migration plus no client-surface, digest-path, or notify-path read. 218 gate assertions green; no SQL changed, so no matrix run.

- 2026-07-11: 4D BUILT, readiness evidence, spec and build the same day (all five gates approved as recommended). Migration 0025 applied live: readiness_evidence behind the LENS WALL (practice-only read, the engagement_drafts discipline; the matrix proves the same-client member reads zero rows and cannot link one), insert and delete through engagement.write with refs validated against the engagement's own artifacts, no update policy (a wrong link is removed, never edited). Each pillar on the panel now carries its evidence list (kind, resolved label, optional why-this-counts, remove) and a scoped picker; execution shows its computed facts from lib/readinessFacts.ts (sessions held in 30 days, on-time versus late completions, review submissions), fixture-gated to never emit a grade word, rendered as history above the judgment; and Share-as-a-reflection seeds a compose box from the three notes and sends through the existing replyMessage path into the one thread. No confidence field anywhere, as gated. 203 gate assertions green; fresh scratch matrix clean. Also this session: the Vercel domain step (app.soboconsulting.com) written into the setup checklist as section 4b after Remi asked why keystone-blue-tau.vercel.app still shows; attaching the domain and the DNS CNAME are his two manual steps.

- 2026-07-11: 4A BUILT and PHASE 4 OPENED, spec and build the same day (all five gates approved as recommended). No migration, as gated: Phase 3 built the sensors and 4A only adds order. lib/actionQueue.ts is the pure gate-tested composition (group order people-paper-intentions-housekeeping; the ready-to-move rule reads 3B's moves pair against the live stage; prep means purpose AND agenda; age in prose, never color; the code provably never says score, percent, or badge). /today now leads with What needs you today: one line per fact with a link (a client waiting on a message or a submission, digests to approve, sessions missing their run of show, named moves the arc has not landed, overdue internal tasks and the stall flag), the 4F inbox and the digest approve/dismiss detail below it, the old sessions/homework/messages cards folded into queue lines with a quiet This-week list at the bottom. An empty queue says the room is quiet. 199 gate assertions green.

- 2026-07-10: 3G BUILT and PHASE 3 CLOSED, spec and build the same day (all five gates approved as recommended). Migration 0024 applied live: digests_client_read admits SENT rows only (the matrix proves a client reads the sent digest and zero approved-but-unsent ones; sessions still write nothing), digest_cadence on engagements (weekly, biweekly, off) checked by the Friday cron BEFORE any model call (off costs nothing; biweekly skips while the last sent digest is under two weeks old; skips logged as counts), and the digest anchor joining messages, closing the 3E deferral. Client: /digests (desktop nav, the Account precedent; tab bar untouched) rendering each sent digest as a document with Ask-about-this, plus the quiet this-week line on Home. Practice: the Digests fold on the engagement page (sent list expandable, the cadence select). Per-recipient modes deferred to their own spec at tenant two; everyone keeps the digest email, the heartbeat, with the archive as its memory. 193 gate assertions green; fresh scratch matrix clean. Phase 3 final tally: 3A, 3B, 3C, 3D, 3E, 3G, 4F pulled forward, 3H from the harvest list, 3F delivered across the library commits; migrations 0017 through 0024; the gate count grew 156 to 193 in the phase.

- 2026-07-10: 3E BUILT, contextual message anchors, spec and build the same day (all five gates approved as recommended). Migration 0023 applied live: anchor_type/anchor_id/anchor_label on messages with the whole-anchor check, sealed at send by the existing 0007 column grant (the matrix asserts a repoint fails); no policy or grant changed. lib/messageAnchors.ts resolves anchors through the CALLER'S OWN session inside the engagement scope and derives the label server-side, so a client member can only anchor what their wall admits and a forged id refuses the send. Entry points: Ask-about-this on client deliverable cards, homework item pages, session detail, and decisions; Message-about-this on practice deliverable cards; each prefills the one composer with a removable chip. Chips render in both threads with side-appropriate links (the CLIENT_HREFS discipline); no composer picker (coming from the artifact IS the picker); digest anchors wait for 3G. 189 gate assertions green; fresh scratch matrix clean.

- 2026-07-10: 3D BUILT, the deliverable lifecycle, spec and build the same day (all five gates approved as recommended). Migration 0022 applied live: about_md and session_id on deliverables, deliverable_versions (append-only, both sides read, only engagement.write inserts; the matrix asserts nobody can rewrite or delete history) and the approval_decided kind. Acceptance rides the 5D approvals machinery with ZERO approval schema change (subject_type deliverable was legal since 0012): the practice requests deliberately per deliverable (the charter one-live-ask discipline), the client accepts or answers not-yet-with-a-note through the same decided-once JWT-stamped path the charter sign-off uses, and the generalized decideApproval now serves both surfaces with a validated back target and the 4F decision emission to the practice. Files replace via signed upload with the outgoing object recorded as a version BEFORE the pointer moves, nothing deleted; version downloads practice-side; the client sees About prose (MarkdownLite), inline View beside Download, version facts, and the calm decide block. The add form gained About and the session select. Viewed-by tracking stays cut, now enforced by a gate that greps the whole tree for it. 185 gate assertions green; fresh scratch matrix clean.

- 2026-07-10: 3B BUILT, the run of show, spec and build the same day (all five gates approved as recommended). Migration 0021 applied live: purpose, agenda_md, the structured moves pair (workstream plus target stage, readable later by 4A and 4E), and reschedule_note on sessions, PLUS the column grant that closes the recon finding: a session on either side may now update exactly the reschedule verbs, and the run-of-show structure is practice-authored through the service role (the matrix asserts the grant from both sides). Practice: the Run of show fold on the session page (purpose line, moves selects from the live workstreams and stage config, the MarkdownEditor agenda, due-before list, the reschedule note surfaced). Client: the run-of-show card on session detail (purpose, the moves line in plain words, the agenda rendered, due-before linking into the homework loop), purpose lines under upcoming sessions on /sessions, and the reworked reschedule flow (one form: pick the time, add an optional word on why). Gate 3B-2 live: a reschedule shifts the session's before/after homework due dates by the same day delta through lib/rescheduleShift.ts, audited as counts. The session reminder joined the 4F cron: one dedupe-keyed touch the day before, both sides, titled by the purpose line. 181 gate assertions green; fresh scratch matrix clean.

- 2026-07-10: 4F BUILT, the notifications layer, spec and build the same day (all five gates approved as recommended). Migration 0020 applied live: notifications behind a RECIPIENT wall (only the owner of the recipient membership reads; the matrix proves a teammate reads zero and that the wall cuts both ways, the practice owner walled from a member's inbox too), read_at as the one session write via column grant, zero session inserts or deletes; notification_prefs, one row per person, the mute. lib/notify.ts is the single writer (the qaExchange service-role-in-lib precedent). Eight emissions wired: homework submitted (to the practice), homework feedback (to the coachee), poll opened and booked (to the team), deliverable shipped, charter sign-off waiting, message replies both directions (in-app rows beside the emails that already existed). The daily cron /api/notify (secret-gated, fails closed, vercel.json 14:00 UTC) materializes the two-touch reminders (due tomorrow, three days overdue, dedupe-keyed) then sends at most ONE email per recipient per day, honoring the mute, never falsely stamping a failed send. Surfaces: "New for you" on client /messages with mark-all-read, the same card on /today, the email toggle on client /account and practice Settings. 177 gate assertions green; fresh scratch matrix clean. Live sends wait on RESEND_API_KEY and CRON_SECRET.

- 2026-07-10: 3A BUILT, the editable AI proposal review, all four gates approved as recommended. Migration 0019 applied live: edited_payload/edited_at/edited_by on ai_proposals plus the immutability trigger, so the AI's original payload cannot be rewritten by ANY writer, service role included (the matrix asserts the trigger fires even for the superuser). The pending extraction on the session notes page became the review workspace: editable summary, decision lines with log toggles (default on, date defaulting to the session, free-prose who), action items shaped per row (edit, assign from either roster, due date, needs-review toggle, disposition: client homework, internal task behind the 3C audience wall, or drop) plus an add-what-the-model-missed row. Save draft writes the edited copy and stays proposed ("edited, unpublished" in the queue); Publish is selective (the note, the decision-log rows through the 0013 columns finally earning their keep, the items) and stamps the edited copy first so the record shows exactly what was reviewed; Dismiss unchanged; accepted never reopens (gate 3A-4). decideProposal shrank to dismiss-only; reviewProposal is the one publish path, every assignee validated against the proposal's own rosters, every string voice-swept. Pure helpers in lib/aiReview.ts, gate-tested. 172 gate assertions green; scratch matrix clean.

- 2026-07-10: 3H BUILT, group scheduling, spec and build the same day (gates approved as recommended). Migration 0018 applied live: session_polls (one open per engagement, enforced by a partial unique index), session_poll_options, session_poll_marks (self-authored, retractable while open, never editable). The practice opens a poll from its OWN offered slots (slot assembly moved to lib/slotAssembly.ts and shared by both surfaces; candidates validated with isOfferedSlot at creation and again at confirm), the client team marks "works for me" with names showing on /sessions, the practice confirms the winner and the booking rides the existing sessions path with the exclusion constraint as the last word; close-without-booking always available. Matrix block covers forged marks, teammate-mark deletion, client poll creation, closed-poll writes, and both classic walls; 168 gate assertions green; fresh scratch matrix run clean.

- 2026-07-10: 3C BUILT, the homework accountability loop, all five gates approved as recommended. Migration 0017 applied live: body_md, review_requested, and audience on action_items (internal practice tasks now invisible to client sessions, the 3A-1 wall transferred in), the tightened check-off (a review item is never self-completed), and homework_activity, the append-only trail whose read policy IS the V2-4 wall (practice or assigned coachee, never a teammate or buyer; the matrix's new member_a1c persona proves the same-client case). Client side: homework cards with state chips, the item page with the thread, submit (note plus link), comment, blocked and clear-the-block, Home strip says "with the consultant" instead of nagging a submitted item. Practice side: the engagement Homework section (awaiting-your-review queue first, internal marked, recently done), the item page with accept and send-back-with-note, the long-missing manual add form (either roster, audience, due, workstream, review toggle), /today counts real submissions. Loop state is DERIVED from the trail (lib/homework.ts, gate-tested state machine), never stored on the readable row. 165 gate assertions green; scratch matrix run clean. Evidence files are the agreed follow-up commit (3C-4); reminders wait for 4F.

- 2026-07-10: the 3C spec written and 3C pulled to the front of Phase 3 on Remi's call ("the homework place needs a major upgrade"). The design: homework grows from a checklist into a coaching loop with two speeds per item (check-off stays for reading-and-doing items; submit, feedback, accept for work that deserves eyes), a real body on every item, comments and evidence in an append-only homework_activity trail, the long-missing manual add form, and the V2-4 buyer wall implemented mechanically: status stays open/done on the item row, the granular loop state derives from the walled activity table, so a teammate or buyer can never read a coachee's revision history by construction. Takes migration 0017 and 3A's audience wall (internal tasks invisible to clients) if gate 3C-5 approves; 3A slides to 0018. Reminders deferred to 4F on purpose. Five CONFIRM gates open.

- 2026-07-10: the library learns fundraising craft, from the bootcamp material Remi brought in (four PDFs plus the funding-rationale article), adapted for nonprofits with all prose original and in the SOBO voice. Three new guides: Making the ask (the four marks of a real ask, request plus reason, the visit ladder, the direct ask and the open plan, the three ways anyone can help), Why this number: the funding rationale (the "X because it will do Y" sentence, unit math, honest proxies for long-arc impact, operations as impact, show your math), and Questions that move the conversation (discovery, alignment, and action questions plus the before-every-visit habit). Three upgrades: the meeting guide gains The open (scripted first two minutes, "what do you know about us?", agenda permission) and impact-then-income sequencing; Donor journeys gains Start with who you already have (the three-mark check on current funders before chasing new names); the AI guide gains Finding funders with it (widen cause words, look sideways at peer funders, deep-dive the shortlist, verify before the CRM). Applied live (12 guides plus the ledger now) and checked in as seed-library-additions.sql. Data only, no code; 156 gate assertions green.

- 2026-07-10: the library grows its hands, on Remi's ask. MarkdownLite now renders bold, italic, safe http links, and numbered lists (the charter's numbered sections and the ledger's bold headers improve for free); authoring gains a real editor (toolbar for headings, bold, italic, lists, links, plus a preview tab rendering exactly what the reader sees) on both the create and edit forms; and resources carry document attachments at last, the storage_path column and the resources bucket idle since Ring 4 finally earning their keep: PDF or Word, 20MB, signed upload direct-to-storage after the check, replace removes the old object, download routes on both surfaces streaming through the session under the path-scoped policy (pure RLS on the client side). 156 gate assertions green.

- 2026-07-10: main fast-forwarded to the test-pass fixes (e4b170c) and deployed. Phase 3 opened with the 3A spec: the editable review workspace between what the AI said and what you publish. The original payload becomes trigger-immutable with edits in a separate copy, decisions flow into the 2B log through the pre-landed 0013 columns, and the internal-task wall (audience column plus the rebuilt client read policy, the 4B kernel) ships in the same PR because an unwalled internal task is a leak. Four CONFIRM gates open.

- 2026-07-10: the test-pass pair, on Remi's walkthrough. (1) Sign-out and accounts for everyone: a POST-only /auth/signout route, a sign-out in the sidebar footer on both surfaces (collapsed rail included), the client /account page (who you are in this room, since when, the door out) on the desktop nav with a quiet reach from the bottom of Home for phones, and a Your account section with sign-out on practice Settings. (2) The nine library guides written in full in Remi's voice (donor journeys, running a fundraising meeting, the weekly rhythm, messaging angles, segmenting the base, foundations versus individuals, multi-year giving, AI in the daily workflow, positive framing): straightforward, actionable, each with a this-week move; applied live (editable at /library/authoring) and checked in as seed-library-guides.sql; the client library reader upgraded to the MarkdownLite renderer so the guides read as documents. Also: four remi+ test personas added to the SafeSpace roster for client-view testing. 156 gate assertions green.

- 2026-07-10: engagement search built; PHASE 2 IS COMPLETE. lib/recordSearch.ts runs eight engagement-scoped ILIKE queries on the caller's session (charter, decisions, notes, outcomes, homework, deliverables, workstream notes, messages), transcript columns untouched and gate-checked, every query's engagement scoping asserted by the gate. The /ask page became "Ask or find" (plain search above the AI ask, clearly labeled); the engagement page's Ask the record section gained the same find box with practice-side hrefs. Pure helpers (cleanTerm, likePattern, snippetAround) unit-tested. No migration, no model, no new walls; 156 gate assertions green.
- 2026-07-10: main fast-forwarded to 2E (c0f532d) and deployed. The engagement-search spec landed, the Phase 2 closer: plain ILIKE keyword search on the caller's own session (the 2E pattern, so the search scope IS the caller's visibility and no index copies anything), snippets grouped by kind, messages in scope and transcripts out, and the /ask page becoming "Ask or find" rather than a sixth nav page. No migration. Three CONFIRM gates open.
- 2026-07-10: 2E built, the fourth AI job. Migration 0016 applied: qa_exchanges, deny-all (RLS on, zero policies), matrix-asserted for every session including the asker themselves. The pure engine (lib/qa.ts) pins the safety shape at the gates: forced submit tool on the qa task, both envelopes guarded (the question is untrusted too), the corpus cap, and the citation rule proven in unit tests, including the case where a confidently-invented answer with fabricated sources collapses to the honest refusal. The corpus builder (lib/qaCorpus.ts) reads on the caller's session under RLS, transcript columns untouched by query and gate-checked. Surfaces: /ask on the client (linked from the Home card) and "Ask the record" on the engagement page, one shared form, honest error states for slow, budget, unconfigured, and failed. Every exchange lands in the deny-all table with model_used. 150 gate assertions green; live answers wait on ANTHROPIC_API_KEY with the other AI features.
- 2026-07-10: main fast-forwarded to 2C (92a191b) and deployed. The 2E Q&A spec landed, written with the care an AI surface owes: the asker's own session builds the corpus under RLS (settling V2-3 structurally), raw transcripts excluded by query per SECURITY.md 4.2, question and record both ride data-not-instructions envelopes, one forced submit tool with post-parse citation validation (the model cannot cite what it was not given), refusal over guessing as a schema field, exchanges landing in a deny-all qa_exchanges table (accountability without a surveillance feed), no conversation memory in v1, and every pre-existing control attached: AI_QA rate limits, spend ceilings, cost ledger, refusal fallback, voice boundary. Four CONFIRM gates open.
- 2026-07-10: 2C built and live. Migration 0015 applied: outcomes (practice writes, both sides read, NO session delete: retiring an outcome is a charter conversation, never a quiet removal) and outcome_evidence (links to the record's four artifact shapes, insert and remove only, never edit). The matrix covers both walls, the client write walls, and the humane deletes, including the assertion that removing an evidence link never touches the artifact; 139 gate assertions green. Practice surface: the Outcomes section on the engagement page with the published charter in a reference fold (the anti-drift mirror), per-row edit with dated standing notes, reached-on as history, and a single grouped evidence picker over the engagement's real deliverables, held sessions, done homework, and decisions, pointer-validated server-side. Client surface: /outcomes ("Where this ends"), linked from the charter page and the Home card; no aggregate display anywhere per gate 2C-2. The eight seed doc section 4 outcomes graduated to rows on the live project, all workstream-tagged, standing notes left for the pilot.
- 2026-07-10: main fast-forwarded to 2D (f00d81a) and deployed. The 2C spec landed: outcomes derived from the charter's success section (the charter prose renders beside the list as the anti-drift mirror), evidence as links to real artifacts only (deliverable, session, action item, decision), standing as dated prose and reached-on as history, NO aggregate progress display anywhere, and the seed doc section 4 table graduating to eight outcome rows on build. Four CONFIRM gates open.
- 2026-07-10: 2D built. "Your next moves" sits at the top of the client Home on every width: pending sign-offs (the charter line is live on day one), the caller's own assigned homework (top three by due), prep items on the next booked session, unread practice replies, and the calm empty state naming the next session. Pure composition under existing walls, session-client reads only, no migration; 134 gate assertions green.
- 2026-07-10: main fast-forwarded to 2F (830ffac) and deployed. The 2D spec landed: "Your Next Moves" as a calm personal strip at the top of the client Home, pure composition of what already exists (pending sign-offs, my assigned homework, prep on the next session, unread replies, the next session), postures answered by data rather than flags, deliverable-review honestly deferred to 3D, practice side explicitly left to 4A. No migration. Three CONFIRM gates open.
- 2026-07-10: 2F built and live. Migration 0014 applied (note_md and note_updated_at on workstreams; no new table, no RLS change; the matrix asserts the client reads the note and can never write it; 134 gate assertions green). The practice engagement page grew a one-line why-we're-here form under each arc (voice-swept, client-visible on save, audited); the client Home grew a quiet details fold under each arc: the stage meaning (new shared copy map in src/lib/stageMeanings.ts), the consultant's note with its date, up to three recent decisions tagged to that workstream, open homework with the nearest due, and the latest deliverable, each linking to its full surface. Session linkage deferred to 3B per gate 2F-2.
- 2026-07-10: main fast-forwarded to 2B (c887d24) and deployed. The 2F spec landed: the "why we're here" note as two columns on workstreams (client-visible on save, voice-swept), the expandable panel under each Home arc composing stage meaning, note, recent decisions, open homework, and latest deliverable from already-walled tables, and session linkage explicitly deferred to 3B's run of show. Three CONFIRM gates open.
- 2026-07-10: 2B built and live. Migration 0013 applied: decisions, the cleanest table in the schema (ZERO update and ZERO delete policies; supersession is a pointer on the successor, so logged means logged structurally), free-prose decided_by_label, client-visible by design, 3A's accepted-proposal columns pre-landed. Matrix grew both scope walls plus the no-rewrite and no-delete assertions on both sides; 132 gate assertions green. Surfaces: the Decision log section on the engagement page (list with superseded strikethrough and revisit dates, voice-swept add form with workstream, revisit, and supersedes picks) and the read-only /decisions history on the client surface, linked from the charter page and the Home charter card. The thirteen July 7 decisions graduated from session_notes prose to rows on the live project: all session-linked, six workstream-tagged, attributed in free prose, decision 11 naming the fee by reference per gate 9. The session note keeps its original prose as the record of the call.
- 2026-07-10: main fast-forwarded to 5D+2A (df500b6) and deployed; the charter and its sign-off block are live on app.soboconsulting.com. The 2B spec landed: decisions as immutable first-class rows (zero update AND zero delete policies, supersession as a pointer on the successor so rows never mutate), free-prose decided-by, client-visible by design with the section 12 wall gating entry, and the thirteen July 7 decisions graduating from session_notes prose to rows on build. Four CONFIRM gates open.
- 2026-07-10: 5D and 2A built and live, the charter proving the approvals primitive end to end. Migration 0012 applied: approvals (immutable once decided, no delete policy anywhere, the decider stamped by DATABASE TRIGGER from the verified JWT so identity can never ride the payload, sessions column-granted to status and note only, pending-only update policies split by side) and engagement_charters (append-only versions, drafts invisible to client members by policy inside the one table, one live published version held by partial unique index, published bodies immutable to every session because the publish transition is service-role-after-check). The live matrix grew both walls including the stamped-decide assertion and the re-decide and rewrite immutability holes; green on scratch Postgres end to end; static gate pins the shapes; 127 gate assertions green. Surfaces: /engagements/[id]/charter (draft with voice sweep, publish supersedes and re-requests sign-off, history), a charter state line on the engagement page, /charter on the client surface (published body via the new MarkdownLite renderer, the sign-off block: approve or not-yet with note through pure RLS, superseded history under a quiet details fold), and the charter card first in the Home rail. The SafeSpace charter GRADUATED on the live project via seed-charter-v1.sql: version 1 published by Remi with the graduation intro stripped and the fee line kept (gate 9), the sign-off request pending for the client, the placeholder library resource removed, and the pilot seed guarded against ever re-inserting it.
- 2026-07-10: Phase 2 opens with two specs. 5D (the Approvals primitive, pulled forward): immutable-once-decided rows naming the human who agreed, "not yet" instead of decline, the client's decide action riding pure RLS through the 0007 column-grant pattern, withdrawn as the practice's own take-back. 2A (the Charter): versioned append-only markdown constitution, drafts invisible to the client by policy inside the one table, one live published version enforced by partial unique index, sign-off through 5D, the seeded SafeSpace charter graduating from its placeholder library resource to charter version 1 on build. Seven CONFIRM gates open across the two.
- 2026-07-10: the Client Agreement Document store built and live (the $25,000 is confirmed; Susan finds the executed agreement waiting on her home). Migration 0011 applied: engagement_documents (doc_type ready for SOWs and invoices later, agreement-only in the UI), visible_to_client false by default, the client read gated on visibility AND own-client membership, the private engagement-documents bucket on the 0006 storage contract. Operator panel on the engagement page (upload the PDF direct-to-storage, view, download, share or stop sharing, remove; every mutation audited); "Your agreement" is the first card in the client home rail with view, download, and the quiet empty state. Matrix extension in the same PR, green on scratch Postgres: unshared invisible to the client (row AND object, even with the exact path), cross-client and cross-practice zero, client write and visibility-flip walls. 120 gate assertions green. **Adaptations from the uploaded spec, per its own CONFIRM-1 instruction to fit the real schema:** (1) client reads ride is_member_of_client, since engagement_members does not exist; (2) client_id is denormalized on the table, the standing law the spec's draft table lacked; (3) client downloads are PURE RLS streamed through the session (the deliverables route pattern), not the spec's session-SELECT-then-service-role-signs broker, because the client surface's no-service-role law is CI-enforced; this also retires the spec's 60-second-signed-URL expiry requirement, since no forwardable URL exists at all on either surface; (4) uploads ride the Ring 4 signed-upload-URL path rather than through a server action, which would hit the request body cap far below 20MB; (5) no new admin client was created, the existing supabaseAdmin chokepoint serves; (6) the spec's arbitrary hex classes map onto the frozen token classes. CONFIRM-2 held (/home exists), CONFIRM-4 held (multiple rows, replace by remove).
- 2026-07-10: main merged to the V2 build and deployed. The branch carried Phase 0 (seed, exclusion wall), the gate decisions, 1A (members and access), and 1B (the engagement builder); main contributed the calendar OAuth trim, merged in cleanly with all 113 gate assertions green. Production deploys from main via the git-linked Vercel project.
- 2026-07-09: V2 1B built and live. Migration 0010 applied: engagement_drafts, practice-only by construction (zero client-facing policies; reads via is_practice_member, writes via the engagement.write authority; no delete policy, discard and restore are status flips). The live matrix grew the draft wall block, with the sharpest case first: a member of the SAME client reads zero drafts about their client; plus cross-practice zero, the client write wall, and the no-delete assertion; green on scratch Postgres 16 end to end, pinned statically by builder-isolation.spec.ts. The surface: a drafts strip on /engagements (New engagement button, resume links, published drafts collapsed as the scoping record) and /engagements/drafts/[id], six sections each saving on its own (client with owner-only inline add enforced by RLS itself, basics with the charter-only fee line, workstreams in the proposal language with stage/reorder, cadence in plain words, invite list, private scoping notes that never publish). Publish validates the full shape then births the engagement (born active), its workstreams, and pending client_members rows, sending invite emails through the shared 1A path (extracted to lib/inviteSend.ts, both surfaces now ride one rate-limited, audited sender) unless the checkbox says wait. 113 gate assertions green; typecheck, lint, build clean.
- 2026-07-09: the 1B spec landed (`specs/keystone-v2-engagement-builder.md`). The load-bearing finding: `engagements.status = 'proposed'` is already client-visible and bookable on the client surface, so a draft flag on the live table would need a filter in every client policy and query, one miss being a leak. The design instead: a practice-only `engagement_drafts` table (jsonb shape, zero client-facing policies, invisible by construction), resumable sections instead of a wizard, and publish as the one validated transaction that births the engagement, workstreams, and pending invites, riding 1A's invite path. Four CONFIRM gates open (invites at publish, inline client add, born-active, drafts kept as the scoping record).
- 2026-07-09: V2 1A built and live, the first V2 feature. Migration 0009 applied to the live project: soft revocation (revoked_at, revoked_by) and invite bookkeeping (invited_by, last_invite_sent_at) on both membership tables, and EVERY membership predicate re-created with the revoked filter (both is_member functions, is_client_member_of_practice, keystone_can on both arms, owns_client_membership, the claim RPC, notify targets), so one revocation closes every wall at once. Both app resolvers filter revoked rows. The live matrix grew a revocation block (revoked reads zero everywhere, cannot write, revoked pending invite cannot claim, reactivation restores exactly the old access) and passed on a scratch Postgres 16 end to end; a new static gate (members-access-isolation.spec.ts) pins the 0009 shapes; 107 gate assertions green. The surface: /settings/members (owner-only; a consultant meets a calm 403): practice team with role change and the last-owner rule (with a post-write restore so concurrent demotions can never end at zero owners), clients and their people, pending invites with rate-limited resend. The invite email is the designed artifact on the new shared emailShell (digest adopted it, one branded frame): no credential in the email, /login prefill, reply-to the inviter. Adds LIMITS.INVITE_SEND_PER_TARGET (1 per 10 min) and INVITE_SEND_PER_HOUR (30 per practice). Every mutation audited, metadata only.
- 2026-07-09: gate day two. Remi adopted the standing recommendations: V2-1 (three to four weeks of pilot; spec during), V2-4 (the buyer-view wall, firmly), V2-5 (pipeline flagged off for SOBO), and the fee half of V2-6 with V1 gate 9 (charter only). Effects applied live and mirrored in the seed files: fee_display set, the charter resource carries the fee line, the decision log published to 'shared' with decision 11 naming the fee by reference. The 1A spec landed as `specs/keystone-v2-admin-ui.md` (members and access; revocation through the membership predicates; the invite email as a designed artifact with no bearer tokens). Pilot harvest list opened with Remi's messaging-and-notifications instinct, routed to the 4F unified-inbox spec.

- 2026-07-09: the practice roster corrected in the live DB (pending rows updated, no schema change): Kendra signs in as kendrasobo@gmail.com and Shannon as shannon@ambitionangels.org; the seeded soboconsulting.com guesses for the two are retired. CONFIRM 2 and CONFIRM 4 decided the same day: the four SafeSpace addresses confirmed exactly as seeded, and Shannon gets a v1 consultant login.
- 2026-07-09: the front door grew a second option: "Continue with Google" under the email form, magic link stays first and is the fail-safe (Remi's call). Spec 6.4 amended in place; SECURITY.md section 3 records the two-doors-one-credential story; the claim RPC needed no change because Google also presents a verified email. Checklist section 3b added: sign-in gets its OWN Google OAuth client (the calendar client's consent screen is Testing-mode with sensitive scopes and would refuse client members). Passwords considered and declined: set and reset both need an email link anyway, so they remove no dependency and add a stuffable credential; recorded in login/actions.ts and SECURITY.md.
- 2026-07-09: main fast-forwarded to the full build (f4dc98b); production is live. Features that need keys stay dormant until the setup checklist sweep.
- 2026-07-09: app.soboconsulting.com is live (CONFIRM 1 decided; domain attached in Vercel by Remi, NEXT_PUBLIC_APP_URL updated). Found and fixed a launch blocker in the same pass: the Vercel project predated the app, its framework preset stuck at "Other", and every deployment served the platform 404 on every path (masked by the SSO wall on preview URLs). "framework": "nextjs" in vercel.json (972688f) fixed it; the login page confirmed serving 200 on the public domain. Built on this branch, in order: `ring0: platform layer` through `ring0: preflight findings`, `ring1: spec amendments`, `ring1: the spine schema, permission authority, and seeded isolation matrix`, `ring1: login, shell, and the client progress view`. The live RLS matrix passes against a scratch Postgres 16; all 35 static gate assertions pass; build and typecheck green.
